const c, t = require("@tools/classes").unpack()

return c({
	name = "Lighting",
	description = "Owns the authored global environment and lighting semantics for one DataModel.",
	since = "0.5.0",

	properties = {
		Ambient = {
			description = "Per-channel ambient contribution applied to lit world surfaces.",
			type = t.Color3,
			unmodified = "gargantuan::Color3(0.2f, 0.2f, 0.2f)",
			overrideRead = "GetAmbient",
			overrideWrite = "SetAmbient",
			serializable = true,
			replicated = true,
			category = "Lighting",
			editorHint = "ColorComponents",
			range = { minimum = 0, maximum = 1 },
		},
		SunColor = {
			description = "Art-directable color of the global directional sun.",
			type = t.Color3,
			unmodified = "gargantuan::Color3(1.0f, 1.0f, 1.0f)",
			overrideRead = "GetSunColor",
			overrideWrite = "SetSunColor",
			serializable = true,
			replicated = true,
			category = "Lighting",
			editorHint = "ColorComponents",
			range = { minimum = 0, maximum = 1 },
		},
		Brightness = {
			description = "Bounded multiplier for direct sun irradiance; it does not scale Ambient or Sky pixels.",
			type = t.Float,
			unmodified = "1.0f",
			overrideRead = "GetBrightness",
			overrideWrite = "SetBrightness",
			serializable = true,
			replicated = true,
			category = "Lighting",
			range = { minimum = 0, maximum = 8 },
		},
		ClockTime = {
			description = "Authored cyclic time in hours within the strict range 0 <= ClockTime < 24.",
			type = t.Float,
			unmodified = "12.0f",
			overrideRead = "GetClockTime",
			overrideWrite = "SetClockTime",
			serializable = true,
			replicated = true,
			category = "Time",
			range = { minimum = 0, maximum = 24 },
		},
		ExposureCompensation = {
			description = "Photographic exposure compensation in stops; the renderer applies 2 raised to this value.",
			type = t.Float,
			unmodified = "0.0f",
			overrideRead = "GetExposureCompensation",
			overrideWrite = "SetExposureCompensation",
			serializable = true,
			replicated = true,
			category = "Exposure",
			range = { minimum = -8, maximum = 8 },
		},
		EnvironmentColor = {
			description = "Visual fallback background color used when no coherent Sky is available.",
			type = t.Color3,
			unmodified = "gargantuan::Color3(0.08f, 0.12f, 0.2f)",
			overrideRead = "GetEnvironmentColor",
			overrideWrite = "SetEnvironmentColor",
			serializable = true,
			replicated = true,
			category = "Environment",
			editorHint = "ColorComponents",
			range = { minimum = 0, maximum = 1 },
		},
		FogEnabled = {
			description = "Whether simple camera-distance linear fog affects world geometry.",
			type = t.Bool,
			unmodified = "false",
			overrideRead = "GetFogEnabled",
			overrideWrite = "SetFogEnabled",
			serializable = true,
			replicated = true,
			category = "Fog",
		},
		FogColor = {
			description = "Color approached by world geometry at FogEnd.",
			type = t.Color3,
			unmodified = "gargantuan::Color3(0.5f, 0.6f, 0.7f)",
			overrideRead = "GetFogColor",
			overrideWrite = "SetFogColor",
			serializable = true,
			replicated = true,
			category = "Fog",
			editorHint = "ColorComponents",
			range = { minimum = 0, maximum = 1 },
		},
		FogStart = {
			description = "Camera distance at or below which no fog is applied.",
			type = t.Float,
			unmodified = "0.0f",
			overrideRead = "GetFogStart",
			overrideWrite = "SetFogStart",
			serializable = true,
			replicated = true,
			category = "Fog",
			range = { minimum = 0, maximum = 100000 },
		},
		FogEnd = {
			description = "Camera distance at or above which world geometry is fully FogColor.",
			type = t.Float,
			unmodified = "1000.0f",
			overrideRead = "GetFogEnd",
			overrideWrite = "SetFogEnd",
			serializable = true,
			replicated = true,
			category = "Fog",
			range = { minimum = 0, maximum = 100000 },
		},
	},
})
